import { Routes } from '@angular/router';
import { SignupPageComponent } from './pages/signup-page/signup-page.component';

export const SIGNUP_ROUTES: Routes = [{ path: '', component: SignupPageComponent }];
